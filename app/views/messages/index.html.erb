<h2>Game Room</h2>
<ul id="chat">
  <% if @messages.any? %>
      <%= render @messages %>
  <% end %>
  </ul>
  <%= form_for (Message.create), remote: true do |f|%>
    <%=f.text_field :content%>
    <%=f.submit "Send"%>
  <%end%>
  <%=subscribe_to "/messages/create" %>
</ul>